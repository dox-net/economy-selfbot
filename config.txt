[Configuration]
# amount of time between tasks (minutes) #
TaskTime = 60
# adds some random delay between tasks #
Randomness = False
# shows the timestamp for when a task was complete #
Timestamps = True
# the channel tasks should be completed #
ChannelID = 000000000000
# you can send multiple messages in your task #
TaskMessages = !work,!dep all
# your discord token #
Token = TOKEN_GOES_HERE
